extends = ["shakespeare_deployed_repro.toml"]

[training]
# Keep the historical deployed Shakespeare recipe, but use the promoted
# short-context CUDA dense-score train path instead of the exact recurrent path.
sequence_kernel_override = "bdh_linear_dense_score_experimental"
